Setup:
    bun install

Run interpretatively:
    bun run start

Compile:
    ./build-platform.sh

    Builds the project as an executable with the bun runtime packaged in.
    Builds node-raylib locally with cmake